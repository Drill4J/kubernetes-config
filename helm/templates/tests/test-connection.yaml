apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "drill-chart.fullname" . }}-test-connection"
  labels:
    {{- include "drill-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "drill-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
